# Build artefacts and fuzzer runtime output. Hand-curated seed inputs
# live under `seeds/<target>/` (tracked); `cargo fuzz run` writes
# minimised mutations into `corpus/<target>/` which is intentionally
# ignored to keep the repo small. See README.md for how to seed a run.
target
artifacts
coverage
corpus
